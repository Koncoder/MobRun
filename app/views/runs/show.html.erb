
<div class="run-banner">
  <div class="run-half-banner run-half-banner-1">
    <div class="run-banner-text">
      <h1><span class="orange-bold-text"><%= @session %> people</span><br>are lacing up for<br><span class="orange-bold-text"><%= @route.name %></span><br>[<%= @route.total_length %>km at <%= @run.speed %>km/h]</h1>
      <%= link_to "Count me in", run_run_sessions_path(@run), method: :post, class: "btn btn-primary btn-lg" %>

    </div>
  </div>
  <div class="run-half-banner run-half-banner-2">
    <div class="run-map-inside">
      <iframe frameBorder="0" scrolling="no" src="https://es.wikiloc.com/wikiloc/spatialArtifacts.do?event=view&id=19439921&measures=on&title=off&near=off&images=off&maptype=M" width="600" height="600"></iframe><div style="background-color:#fff;color:#777;font-size:11px;line-height:16px;">Powered by <a style="color:#06d;font-size:11px;line-height:16px;" target="_blank" href="https://es.wikiloc.com">Wikiloc</a></div>
    </div>
  </div>
</div>

<div class="container run-container">
  <p class="text-right">This run was created by <%= @user.name %> <%= cl_image_tag current_user.profile_picture.url(:bright_face), height: 35, width: 35, crop: :fill, class: "avatar dropdown-toggle", id:"navbar-wagon-menu" %> </p>
  <div class="row">
    <div class="col-xs-12 col-sm-offset-2 col-sm-8 text-center run-text">
       <% if @run.end_time.past?  %>
        <h1><%= @session %> people crossed the finish line together. Click here to lace up for this route!</h1>
      <% elsif @run.start_time.past?  %>
        <h1><%= @session %> people are on their way to <span class="orange-bold-text"><%= @route.end_point %></span> now!</h1>
      <% elsif @run.start_time.today? %>
        <h1>This run begins in <%= Time.at(@run.start_time - Time.now).utc.strftime("%H:%M:%S") %></h1>
      <% else %>
        <h1>This run begins on <span class="orange-bold-text"><%= @run.start_time.day %>/<%= @run.start_time.month %></span> </h1>
      <% end %>
    </div>
  </div>
  <hr>
</div>
